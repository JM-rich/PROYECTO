<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.jrmg.proyjrm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecomendacionActivity"></activity>
        <activity android:name=".AcercadeActivity" />
        <activity android:name=".EvoluquintaActivity" />
        <activity android:name=".EvolucuatroActivity" />
        <activity android:name=".EvolutresActivity" />
        <activity android:name=".EvoludosActivity" />
        <activity android:name=".EvolunoActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>